<div id="ingredients">
  <hr>
  <strong>Ingredients:</strong>
  <ul id="ingredients-list">
    <% @cocktail.doses.each do |dose| %>
      <%= render 'doses/show', cocktail: @cocktail, dose: dose %>
    <% end %>
  </ul>
</div>
